# 引き継ぎ書：音声ジャーナル 自動記録システム

作成日：2026/05/22

---

## 1. システム概要

Googleドキュメントに音声入力した児童の観察記録を、毎週金曜16:00に自動で読み取り、Macローカルの Excelファイルに追記するシステム。

```
音声ジャーナル（Googleドキュメント）
        ↓ 毎週金曜 16:00 自動実行
    Claude CLI（Mac）
        ↓ MCP経由で読み取り
  JSON形式で新規エントリを抽出
        ↓ Pythonスクリプトで書き込み
所見記録.xlsx（デスクトップ/クロコタスク）
```

---

## 2. ファイル一覧

### Mac上のファイル

| ファイル | 場所 | 役割 |
|---|---|---|
| `所見記録.xlsx` | `~/Desktop/クロコタスク/` | 出力先Excel（観察記録・道徳記録の2シート） |
| `journal_update.sh` | `~/Library/Scripts/` | 自動実行シェルスクリプト |
| `update_excel.py` | `~/Library/Scripts/` | ExcelへのJSON書き込みスクリプト |
| `週次所見まとめ.md` | `~/claude-work/.claude/commands/` | Claudeへの指示（スラッシュコマンド定義） |
| `last_matome_date.txt` | `~/claude-work/` | 前回まとめ日を記録（差分管理用） |
| `settings.json` | `~/.claude/` | Claude CLI の権限設定 |
| `com.teacher.journal-update.plist` | `~/Library/LaunchAgents/` | 毎週金曜16:00の自動実行設定 |

### Google Drive上のファイル

| ファイル | 用途 |
|---|---|
| R8音声ジャーナリング（Doc） | 音声入力の記録元。ID: `1L3FSmWiGNmoLeWv2rBYCXGQDtqXK3GIdHGDvG-J6Fho` |
| R8 音声ジャーナル 道徳記録まとめ（Sheets） | 道徳記録のクラウドバックアップ。ID: `1D4fkvTZMZt6tTMBdbwtueXW8alXXV09GQIJN2LNAoJE` |

---

## 3. 音声入力のルール

### 通常の観察記録
そのまま番号と内容を入力する。
```
例：3番 友達と話し合いながら課題に取り組んでいた
```

### 道徳の記録
冒頭に `【道徳】` をつける（通知表の総合所見には使わない）。
```
例：【道徳】5番 友達を助ける場面で積極的に行動していた
```

### 音声誤認識の修正
Claudeが自動修正する（レッドバーペット→レッドカーペット など）。

---

## 4. 日常の操作

### 自動実行（何もしなくてOK）
毎週金曜16:00にMacが自動で実行する。Macがスリープ中だと実行されないので、金曜の夕方はMacを起動しておく。

### 手動実行
```bash
bash ~/Library/Scripts/journal_update.sh
```

### ログの確認
```bash
cat ~/Library/Logs/journal_update/$(date +%Y-%m-%d).log
```

### 実行結果の見方
- ✅ `Excel更新完了` → 正常終了
- `観察記録: N件追記` → 新しく追加されたエントリ数
- ⚠️ `記録なし` → 一度も記録がない出席番号
- 🕐 `1ヶ月以上記録なし` → 要フォローの出席番号

---

## 5. Excelファイルの構成

**シート1：観察記録**（通知表の総合所見に使用）

| 出席番号 | 観察記録（箇条書き） | 最終記録日 | まとめ日 | 備考 |
|---|---|---|---|---|
| 1〜29 | 自動追記 | 自動更新 | 自動更新 | |

**シート2：道徳記録**（道徳の評価に使用、総合所見には使わない）

同じ列構成。

---

## 6. 注意事項

- **個人名は記録しない**（出席番号のみで管理）
- **データは外部共有禁止**（Google Drive 内とローカルMacのみ）
- 29番はまだ記録なし（記入されたら自動的に反映される）
- 7番は記録が少ない（意識的に観察・記録を）

---

## 7. トラブルシューティング

### スクリプトが止まって進まない
→ `Ctrl+C` で止めて、ログを確認する。その後 `bash ~/Library/Scripts/journal_update.sh` で再実行。

### `claude: command not found` エラー
→ launchdのパス問題。スクリプト内の `CLAUDE=/usr/local/bin/claude` を確認する。

### Excelが更新されない（0件追記）
→ `~/claude-work/last_matome_date.txt` の日付を確認。新しい記録より前の日付になっているか確認する。

### 新しい記録が反映されない
→ `last_matome_date.txt` を古い日付に書き換えて再実行する。
```bash
echo "5/1" > ~/claude-work/last_matome_date.txt
bash ~/Library/Scripts/journal_update.sh
```

---

## 8. 参照ドキュメント

- `~/claude-work/通知表_用語統一リスト.md`：通知表で使う言葉・漢字の統一リスト
- `~/claude-work/.claude/commands/週次所見まとめ.md`：Claudeへの指示内容（変更時はMacのsedコマンドで更新）

---

## 9. 定期メンテナンス

| 頻度 | 作業 |
|---|---|
| **月1回** | Google Driveで古い「R8 音声ジャーナル 児童所見まとめ」を検索し、最新以外をゴミ箱へ |
| **学期末** | 所見記録.xlsxをバックアップ保存 |
